apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: morse-code-display-trigger
spec:
  broker: default
  filter:
    attributes:
      source: morse-code-decoded
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: cloudevents-player